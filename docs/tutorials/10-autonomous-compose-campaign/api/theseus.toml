version = 1

[runtime]
firecracker = "runtime/firecracker"

[guest]
kernel = "guest/vmlinux"
initramfs = "guest/initramfs.cpio.gz"

[run]
seed = 71
vcpu_count = 1
mem_size_mib = 128
timeout_secs = 10

[network]
loopback = false
latency_rounds = 1
